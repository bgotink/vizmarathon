 var dialog = {};
 
 dialog.width = 500;
 dialog.height = 400;
 
 dialog.init = function(){
 
 dialog.gbox = map.svg.append("svg:g")
				.attr("id", "gbox")
				.attr("transform","translate("+map.w/2;
 
 dialog.welcome = function(){
 
 }
 
 }